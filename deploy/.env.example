# Zkopíruj na `.env` a vyplň. Soubor `.env` se NIKDY necommituje (viz .gitignore).
# V produkci hodnoty dodá deploy (GitHub Actions secrets / Ansible), ne tento soubor.

POSTGRES_DB=pathogens
POSTGRES_USER=portal
POSTGRES_PASSWORD=change-me-strong

# LLM_API_KEY=
