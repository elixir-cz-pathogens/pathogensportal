# Copy to `.env` and fill it in. The `.env` file is NEVER committed (see .gitignore).
# In production the values are supplied by the deploy (GitHub Actions secrets /
# Ansible), not by this file.

POSTGRES_DB=pathogens
POSTGRES_USER=portal
POSTGRES_PASSWORD=change-me-strong

# LLM_API_KEY=
