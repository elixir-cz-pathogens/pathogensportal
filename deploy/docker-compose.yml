name: pathogensportal

# The production stack. The FE (Hugo) is served by Apache on the host — there is no container for it.
# The services listen on the internal network only; everything goes out through the Apache reverse proxy.
# Nothing is published to the host (no ports:) — the DB is not reachable from outside.

services:
  website-be:
    build: ../backend/website-be
    restart: unless-stopped
    env_file: [.env]
    environment:
      DB_HOST: pathogen-db
    volumes:
      # Vstup pro ETL `python -m app.loader` (chart JSON -> Postgres). Read-only.
      - ../frontend/static/data/charts:/charts:ro
    depends_on:
      pathogen-db:
        condition: service_healthy
    networks: [internal]

  llm-agent-be:
    build: ../backend/llm-agent-be
    restart: unless-stopped
    env_file: [.env]
    networks: [internal]

  mcp:
    build: ../backend/mcp
    restart: unless-stopped
    env_file: [.env]
    networks: [internal]

  pathogen-db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
      # the schema from the pathogensportal-db submodule
      - ../pathogensportal-db/db/init.sql:/docker-entrypoint-initdb.d/01_init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks: [internal]

  # The data pipeline from the pathogensportal-db submodule.
  # It does not run continuously — it is started on demand / from cron:
  #   docker compose --profile tools run --rm datascrapper
  datascrapper:
    profiles: ["tools"]
    build: ../pathogensportal-db
    environment:
      DATA_DIR: /data
      OUTPUT_DIR: /output/charts
      # Added in v0.1.0 (PPDB-24). ⚠️ NOT optional: without it process_ebola.py
      # falls back to /output/content INSIDE the container, writes the Ebola pages
      # to the ephemeral layer and they vanish on exit — no error, no warning.
      CONTENT_DIR: /output/content
    volumes:
      - scraped-data:/data
      # the generated chart JSON goes straight into the frontend (Model A: it is committed)
      - ../frontend/static/data/charts:/output/charts
      # ⚠️ WRITABLE, and it overwrites the eight hand-written ebola-*.md pages on
      # every run. That is intended (the pipeline owns them from v0.1.0 on), but it
      # is why Model A matters here: the overwrite lands as a reviewable git diff
      # rather than silently on a server.
      - ../frontend/content/cs/dashboards:/output/content
    networks: [internal]

volumes:
  pgdata:
  scraped-data:

networks:
  internal:
